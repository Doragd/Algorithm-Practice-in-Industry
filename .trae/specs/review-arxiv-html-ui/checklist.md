- [x] 报告新增在 `docs/` 目录，文件名明确表达 `generate_arxiv_html.py`、HTML UI 或日历 review 主题。
- [x] 报告完整梳理 `generate_arxiv_html.py` 的输入、数据转换、模板渲染、静态资源复制、HTML 输出和 `index.html` 重定向逻辑。
- [x] 报告明确分析日历逻辑，包括当前日期解析、可用日期列表、localStorage、跨月切换、可点击日期、选中态和跳转 URL。
- [x] 报告明确分析页面交互，包括“全部论文/仅显示精选”、非精选折叠、低分论文展开、摘要详情、按钮状态和显示计数。
- [x] 报告明确分析展示逻辑，包括精选判断、排序、评分、平均分、标题/翻译/摘要/理由 fallback、作者截断、分类标签和空状态。
- [x] 报告从功能整体角度分析信息架构、阅读路径、历史浏览、发现能力、筛选能力、移动端体验和长期可维护性。
- [x] 报告包含新的功能 idea，并按近期可做、中期增强、远期探索标注收益、复杂度和风险。
- [x] 报告列出发现的问题或可提升点，并包含严重度、证据、影响和建议方案。
- [x] 验证过程不触发 DeepSeek、不发送飞书、不覆盖线上 JSON/HTML 输出、不依赖生产 secrets。
- [x] `tasks.md` 中所有任务完成后均被勾选。
